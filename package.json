{
  "name": "hello",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.1.7",
    "@biomejs/biome": "^1.6.0",
    "@types/react": "^18.2.64",
    "@types/react-dom": "^18.2.21",
    "@vitejs/plugin-react-swc": "^3.6.0",
    "typescript": "^5.4.2",
    "vite": "^5.1.5"
  },
  "scripts": {
    "dev": "vite",
    "start": "vite preview",
    "start:api": "swa start build --api-location api",
    "build": "tsc && vite build",
    "format": "pnpm dlx @biomejs/biome format src/*",
    "lint": "pnpm dlx @biomejs/biome lint src/*",
    "check": "pnpm dlx @biomejs/biome check --apply src/*",
    "preview": "vite preview"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": "18.x"
  }
}
